import ConnectDB from "../../../utils/connect"

const TopicName = [
    'Psychology',
    'Criminology',
    'Economics',

]

// TODO: feed topics on db and implement

export default async function feedTopics() {
    await ConnectDB()
    try {

    } catch (error) {

    }
}